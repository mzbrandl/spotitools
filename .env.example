# Spotify App credentials (from developer dashboard)
SPOTIFY_CLIENT_ID=your_client_id
SPOTIFY_CLIENT_SECRET=your_client_secret

# Redirect URI — must match one of the URIs registered in Spotify dashboard
# Spotify requires HTTPS except for loopback IPs (127.0.0.1), where HTTP is
# allowed but the port must always be explicit. "localhost" is not permitted.
# Leave unset to auto-derive from the request (recommended for local dev).
# REDIRECT_URI=https://www.spotitools.com/callback

# Internal server port (nginx proxies to this, don't expose directly)
PORT=3001
