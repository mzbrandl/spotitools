# Spotify App credentials (from developer dashboard)
SPOTIFY_CLIENT_ID=a1b90597cc8449c89089422a31b8bfa1
SPOTIFY_CLIENT_SECRET=your_client_secret_here

# Redirect URI — must match one of the URIs registered in Spotify dashboard
# Local Docker:  http://localhost/callback/
# Production:    https://www.spotitools.com/callback/
REDIRECT_URI=http://localhost/callback/

# Internal server port (nginx proxies to this, don't expose directly)
PORT=3001
